# DEMO Property Management

Static company website for DEMO, a property-management company.

## Deployment

The `Deploy DEMO website` workflow publishes the contents of this repository to GitHub Pages on every push to `main`. It uses the repository's Pages deployment environment and requires the repository to remain private with Actions enabled.
